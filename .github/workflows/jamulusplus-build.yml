name: JamulusPlus Build

on:
  workflow_dispatch:
  push:
    branches:
      - JamulusPlus
      - jamulusplus
    tags:
      - 'V*'
    paths:
      - '.github/workflows/jamulusplus-build.yml'
      - '.gitmodules'
      - 'JamulusPlus/**'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/jamulusplus-build.yml'
      - '.gitmodules'
      - 'JamulusPlus/**'

permissions:
  contents: write

jobs:
  build-windows:
    name: Windows VST3 + Standalone
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Checkout JamulusPlus JUCE submodule
        run: |
          git submodule sync -- JamulusPlus/third_party/JUCE
          git submodule update --init --depth 1 JamulusPlus/third_party/JUCE

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        run: >
          cmake -S JamulusPlus -B JamulusPlus/b -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DENABLE_STANDALONE=ON

      - name: Build
        run: >
          cmake --build JamulusPlus/b --config Release
          --target jamulus_engine_VST3 jamulus_engine_Standalone

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: JamulusPlus-windows-vst3
          if-no-files-found: error
          path: JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/VST3/JamulusPlus.vst3

      - name: Upload Standalone
        uses: actions/upload-artifact@v4
        with:
          name: JamulusPlus-windows-standalone
          if-no-files-found: error
          path: |
            JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/Standalone/**
            JamulusPlus/dist/JamulusPlus-Standalone/**

      - name: Package Release Assets
        if: startsWith(github.ref, 'refs/tags/V')
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release-assets | Out-Null
          Compress-Archive -Path "JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/VST3/JamulusPlus.vst3" -DestinationPath "release-assets/JamulusPlus-${{ github.ref_name }}-windows-vst3.zip" -Force
          if (Test-Path "JamulusPlus/dist/JamulusPlus-Standalone") {
            Compress-Archive -Path "JamulusPlus/dist/JamulusPlus-Standalone" -DestinationPath "release-assets/JamulusPlus-${{ github.ref_name }}-windows-standalone.zip" -Force
          } else {
            Compress-Archive -Path "JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/Standalone/*" -DestinationPath "release-assets/JamulusPlus-${{ github.ref_name }}-windows-standalone.zip" -Force
          }

      - name: Publish Release Assets
        if: startsWith(github.ref, 'refs/tags/V')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            release-assets/JamulusPlus-${{ github.ref_name }}-windows-vst3.zip
            release-assets/JamulusPlus-${{ github.ref_name }}-windows-standalone.zip

  build-macos:
    name: macOS VST3 + AU + Standalone
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Checkout JamulusPlus JUCE submodule
        run: |
          git submodule sync -- JamulusPlus/third_party/JUCE
          git submodule update --init --depth 1 JamulusPlus/third_party/JUCE

      - name: Configure
        run: >
          cmake -S JamulusPlus -B JamulusPlus/b -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DENABLE_AU=ON
          -DENABLE_STANDALONE=ON

      - name: Build
        run: >
          cmake --build JamulusPlus/b --config Release
          --target jamulus_engine_VST3 jamulus_engine_AU jamulus_engine_Standalone

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: JamulusPlus-macos-vst3
          if-no-files-found: error
          path: JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/VST3/JamulusPlus.vst3

      - name: Upload AU
        uses: actions/upload-artifact@v4
        with:
          name: JamulusPlus-macos-au
          if-no-files-found: error
          path: JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/AU/JamulusPlus.component

      - name: Upload Standalone
        uses: actions/upload-artifact@v4
        with:
          name: JamulusPlus-macos-standalone
          if-no-files-found: error
          path: |
            JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/Standalone/JamulusPlus.app
            JamulusPlus/dist/JamulusPlus.app

      - name: Package Release Assets
        if: startsWith(github.ref, 'refs/tags/V')
        run: |
          mkdir -p release-assets
          ditto -c -k --sequesterRsrc --keepParent "JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/VST3/JamulusPlus.vst3" "release-assets/JamulusPlus-${{ github.ref_name }}-macos-vst3.zip"
          ditto -c -k --sequesterRsrc --keepParent "JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/AU/JamulusPlus.component" "release-assets/JamulusPlus-${{ github.ref_name }}-macos-au.zip"
          if [ -d "JamulusPlus/dist/JamulusPlus.app" ]; then
            ditto -c -k --sequesterRsrc --keepParent "JamulusPlus/dist/JamulusPlus.app" "release-assets/JamulusPlus-${{ github.ref_name }}-macos-standalone.zip"
          else
            ditto -c -k --sequesterRsrc --keepParent "JamulusPlus/b/plugin/jamulus_engine_artefacts/Release/Standalone/JamulusPlus.app" "release-assets/JamulusPlus-${{ github.ref_name }}-macos-standalone.zip"
          fi

      - name: Publish Release Assets
        if: startsWith(github.ref, 'refs/tags/V')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            release-assets/JamulusPlus-${{ github.ref_name }}-macos-vst3.zip
            release-assets/JamulusPlus-${{ github.ref_name }}-macos-au.zip
            release-assets/JamulusPlus-${{ github.ref_name }}-macos-standalone.zip
