name: Verify translations

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "src/translation/wininstaller/**"
      - "src/translation/*.ts"                     # <-- ADD THIS
      - "tools/check-wininstaller-translations.sh"
      - "tools/check-translations.py"              # <-- ADD THIS (Match your script name)
      - ".github/workflows/translation-check.yml"
  push:
    paths:
      - 'src/translation/wininstaller/**'
      - 'src/translation/*.ts'
      - 'tools/check-wininstaller-translations.sh'
      - 'tools/check-translations.py'              # <-- ADD THIS HERE TOO
      - '.github/workflows/translation-check.yml'

jobs:
  translation-check:
    name: Verify translation
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
      - name: "Check Windows installer translations"
        run: ./tools/check-wininstaller-translations.sh
      - name: "Check for duplicate hotkeys (will not fail)"
        run: perl ./tools/checkkeys.pl
      - name: "Check application translations"
        run: ./tools/check-translations.py
